# ============================================================
# Phil — variables d'environnement
# Copier en .env.local pour le dev local (jamais commité).
# Les valeurs réelles vivent dans Vercel (Settings → Environment Variables)
# pour les trois environnements : production, preview, development.
# ============================================================

# --- Supabase (PHIL-A03) ---
# Dashboard : https://supabase.com/dashboard/project/xinbuahgscaydpkcamsl
NEXT_PUBLIC_SUPABASE_URL=           # https://xinbuahgscaydpkcamsl.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=      # Clé anon publique (protégée par RLS)
SUPABASE_SERVICE_ROLE_KEY=          # Server only — bypasse RLS, JAMAIS côté client

# --- Application ---
NEXT_PUBLIC_APP_URL=                # Prod : https://phil-phi-nine.vercel.app — dev : http://localhost:3000

# --- Google OAuth ---
# Rien ici : le client ID/secret Google sont configurés dans Supabase
# (Authentication → Sign In / Providers → Google), pas dans l'application.

# --- Email Resend (Phase 7, PHIL-K01) ---
RESEND_API_KEY=
RESEND_FROM_EMAIL=                  # onboarding@resend.dev en dev, domaine vérifié ensuite

# --- Rate limiting Upstash (optionnel, PHIL-J02) ---
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=

# --- Import de réservations (PHIL-O01) ---
# Clé Google AI Studio (free tier) : https://aistudio.google.com/apikey
# Sans clé, l'import est simplement désactivé (saisie manuelle inchangée).
# Caveat free tier : Google peut utiliser les données envoyées pour améliorer
# ses modèles — n'y passent que des confirmations de résa, jamais le coffre.
GEMINI_API_KEY=
GEMINI_MODEL=                       # défaut : gemini-2.5-flash

# --- Import par email (PHIL-P02) ---
# Secret partagé du webhook /api/inbound-email — transmis par le provider inbound
# via l'en-tête `x-webhook-secret` ou `Authorization: Bearer` (PHIL-Q43, plus en
# query string). À configurer quand un domaine sera branché ; sinon inactif.
INBOUND_EMAIL_SECRET=
NEXT_PUBLIC_INBOUND_EMAIL_DOMAIN=   # ex : import.phil.app (affiché dans les Paramètres)

# --- Crons Vercel (PHIL-E07/N08) ---
# Protège /api/cron/* : Vercel Cron envoie `Authorization: Bearer $CRON_SECRET`.
CRON_SECRET=

# --- Push Web (VAPID, PHIL-K05) ---
NEXT_PUBLIC_VAPID_PUBLIC_KEY=       # Clé publique VAPID (exposée au client, normal)
VAPID_PRIVATE_KEY=                  # Server only
