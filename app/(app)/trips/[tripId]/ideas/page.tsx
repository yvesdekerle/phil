import { TabPlaceholder } from "@/components/trips/tab-placeholder";

export default function TripIdeasPage() {
  return (
    <TabPlaceholder
      title="Les idées arrivent bientôt"
      hint="Le carnet d'envies du groupe : on propose, on vote, on planifie. (Phase 6)"
    />
  );
}
